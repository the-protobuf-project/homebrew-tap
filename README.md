# On House Homebrew tap - brewing here

[![CI](https://github.com/the-protobuf-project/homebrew-tap/actions/workflows/ci.yml/badge.svg)](https://github.com/the-protobuf-project/homebrew-tap/actions/workflows/ci.yml)

This repository is a personal Homebrew tap for distributing `finfo`.

The list below is generated from `Casks/` and should be kept in sync by the bot.

## Install

```bash
brew tap the-protobuf-project/tap
brew install --cask finfo
```

## Available Casks

<!-- BEGIN GENERATED CASK LIST -->
- `protoc-gen-protorm`
<!-- END GENERATED CASK LIST -->

## Updating

The cask in `Casks/protoc-gen-protorm.rb` is generated by GoReleaser. Regenerate it from the upstream release pipeline instead of editing it by hand.

When a new cask is added or an existing one changes, the bot updates the generated list in this README.
